FROM python:3.6

RUN apt update
{}
RUN mkdir -p /work
ADD ./ /work
WORKDIR /work
RUN pip config set global.index-url http://mirrors.aliyun.com/pypi/simple/
RUN pip config set install.trusted-host mirrors.aliyun.com
{}
CMD python -u vsource_service.py
